apiVersion: "autoscaling.k8s.io/v1beta2"
kind: VerticalPodAutoscaler
metadata:
  name: gardener-seed-admission-controller-vpa
  namespace: {{ .Release.Namespace }}
  labels:
    app: gardener
    role: seed-admission-controller
spec:
  {{- if .Values.gardenerSeedAdmissionController.vpa }}
  {{- if .Values.gardenerSeedAdmissionController.vpa.resourcePolicy }}
  resourcePolicy:
    containerPolicies:
      - containerName: '*'
        minAllowed:
          cpu: {{ required ".Values.gardenerSeedAdmissionController.vpa.resourcePolicy.minAllowed.cpu is required" .Values.gardenerSeedAdmissionController.vpa.resourcePolicy.minAllowed.cpu }}
          memory: {{ required ".Values.gardenerSeedAdmissionController.vpa.resourcePolicy.minAllowed.memory is required" .Values.gardenerSeedAdmissionController.vpa.resourcePolicy.minAllowed.memory }}
  {{- end }}
  {{- end }}
  targetRef:
    apiVersion: {{ include "deploymentversion" . }}
    kind: Deployment
    name: gardener-seed-admission-controller
  updatePolicy:
    updateMode: "Auto"
