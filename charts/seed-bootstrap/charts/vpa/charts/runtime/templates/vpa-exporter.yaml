{{- if .Values.exporter.enabled  }}
apiVersion: "autoscaling.k8s.io/v1beta2"
kind: VerticalPodAutoscaler
metadata:
  name: vpa-exporter-vpa
  namespace: {{ .Release.Namespace }}
spec:
  {{- if .Values.exporter.vpa }}
  {{- if .Values.exporter.vpa.resourcePolicy }}
  resourcePolicy:
    containerPolicies:
      - containerName: '*'
        minAllowed:
          cpu: {{ required ".Values.exporter.vpa.resourcePolicy.minAllowed.cpu is required" .Values.exporter.vpa.resourcePolicy.minAllowed.cpu }}
          memory: {{ required ".Values.exporter.vpa.resourcePolicy.minAllowed.memory is required" .Values.exporter.vpa.resourcePolicy.minAllowed.memory }}
  {{- end }}
  {{- end }}
  targetRef:
    apiVersion: {{ include "deploymentversion" . }}
    kind: Deployment
    name: vpa-exporter
  updatePolicy:
    updateMode: "Auto"
{{- end }}
