{{- if .Values.recommender.enabled }}
apiVersion: "autoscaling.k8s.io/v1beta2"
kind: VerticalPodAutoscaler
metadata:
  name: vpa-recommender
  namespace: {{ .Release.Namespace }}
spec:
  {{- if .Values.recommender.vpa }}
  {{- if .Values.recommender.vpa.resourcePolicy }}
  resourcePolicy:
    containerPolicies:
      - containerName: '*'
        minAllowed:
          cpu: {{ required ".Values.recommender.vpa.resourcePolicy.minAllowed.cpu is required" .Values.recommender.vpa.resourcePolicy.minAllowed.cpu }}
          memory: {{ required ".Values.recommender.vpa.resourcePolicy.minAllowed.memory is required" .Values.recommender.vpa.resourcePolicy.minAllowed.memory }}
  {{- end }}
  {{- end }}
  targetRef:
    apiVersion: {{ include "deploymentversion" . }}
    kind: Deployment
    name: vpa-recommender
  updatePolicy:
    updateMode: "Auto"
{{- end }}
